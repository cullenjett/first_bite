= bootstrap_form_for @product, :html => { :multipart => true } do |f|
  = f.text_field :title
  = f.text_area :description
  = f.text_field :price
  = f.file_field :image
  = f.form_group do
    = f.submit "#{button_content}"
